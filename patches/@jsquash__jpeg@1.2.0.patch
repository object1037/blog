diff --git a/decode.js b/decode.js
index 7be2f1de30bbd3d4f91a438abed884530f15b0c9..57411e0799d6bde5ed12eeec86dce70b3c2fd542 100644
--- a/decode.js
+++ b/decode.js
@@ -14,6 +14,11 @@ import { initEmscriptenModule } from './utils';
 import mozjpeg_dec from './codec/dec/mozjpeg_dec';
 let emscriptenModule;
 export async function init(module) {
+    if (typeof module === 'string') {
+        module = await fetch(module)
+            .then((response) => response.arrayBuffer())
+            .then((bytes) => new WebAssembly.Module(bytes))
+    }
     emscriptenModule = initEmscriptenModule(mozjpeg_dec, module);
 }
 export default async function decode(buffer) {
