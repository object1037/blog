---
title: "test" description: ""
---

export const meta = {
  title: "test",
  date: "20210408"
}

import Layout from "../../components/layout"
import Im from "../../components/im"

### test

>this is a test quote

this is a normal text

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  int a;
  cin >> a;
  for (int i; i < 100; i++) {
    a /= 2;
  }
  return a;
}
```

image test

<Im src="/images/profile.jpg" width="180" height="180" />

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
